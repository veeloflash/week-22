# Product Markdown Fixture

Markdown documents are parsed as UTF-8 text, split into overlapping chunks, embedded, and indexed with source metadata.

## Acceptance note

This fixture exists so the Product dataset includes a directly uploadable Markdown example alongside TXT and PDF files.
